首先加载一个mask图片
mask!=0意味着是有光照信息的部分
将其赋给类中的 self.foreground_ind，表示前景信息
rps.foreground_ind 一维向量，20317个像素点

然后加载光照文件
光照文件应该是 50 行 3 列的矩阵，表示 50 张图片，3 个维度
赋值给self.L

总像素个数：65536

问题：
需要构建包含所有像素点的矩阵
第一次跑代码，self.N中仅有前景图片的信息，导致维度不匹配
背景信息均为0，只需初始化一个包含所有像素点的全0矩阵即可
计算完self.N，将其嵌入这个矩阵即可

